# CARLA client side (runs in the Py3.8 venv: ~/projects/parking_data_gen/venv).
# Imports ParkingScenes; never forks it. Controller adapter, slot-pick UI,
# live viewer, metrics, and the bridge client to the model server.
